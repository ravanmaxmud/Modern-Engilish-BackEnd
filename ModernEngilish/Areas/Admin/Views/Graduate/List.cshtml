@using ModernEngilish.Areas.Admin.ViewModels.Graduate;
@model List<ListItemViewModel>

<div>
    <div class="row">
        <div class="col-sm-12">
            <div class="page-title-box">
                <div class="float-end">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                        </li><!--end nav-item-->
                        <li class="breadcrumb-item active">Galleries</li>
                    </ol>
                </div>
                <h4 class="page-title">Galleries</h4>
            </div><!--end page-title-box-->
        </div><!--end col-->
    </div>
</div>

<div class="col-lg-12">
    <div class="row justify-content-end">
        <div class="">
            <a asp-route="admin-graduate-add" class="btn btn-secondary">Add new Graduate</a>
        </div>
    </div>
</div>

<div class="col-lg-12">
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Image</th>
                            <th>Name</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var blogFile in Model)
                        {
                            <tr>
                                <td><img src="~/@blogFile.ImageUrl" width="10%" /></td>
                                <td class="text-end">
                                    <form method="POST" asp-route="admin-graduate-delete" asp-route-id="@blogFile.Id" id="book-image-form-@blogFile.Id"></form>
                                    <button type="submit" form="book-image-form-@blogFile.Id">
                                        <i class="las la-trash-alt text-secondary font-16"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div>